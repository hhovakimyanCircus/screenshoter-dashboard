# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
env:
  NEXT_PUBLIC_FIREBASE_PUBLIC_API_KEY: '${{ secrets.FIREBASE_PUBLIC_API_KEY }}'
  NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: '${{ secrets.FIREBASE_AUTH_DOMAIN }}'
  NEXT_PUBLIC_FIREBASE_PROJECT_ID: '${{ secrets.FIREBASE_PROJECT_ID }}'
  NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: '${{ secrets.FIREBASE_STORAGE_BUCKET }}'
  NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: '${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}'
  NEXT_PUBLIC_FIREBASE_APP_ID: '${{ secrets.FIREBASE_APP_ID }}'
  NEXT_PUBLIC_EXTENSION_ID: '${{ secrets.EXTENSION_ID }}'
'on': pull_request
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: yarn && yarn build && yarn export
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SCREENSHOTER_DFCD1 }}'
          projectId: screenshoter-dfcd1
